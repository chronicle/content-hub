name: Windows Workflows
permissions:
  contents: read
  pull-requests: read
on:
  push:
    branches:
    - '**'
  pull_request:
    branches:
    - '**'

jobs:
  changes:
    runs-on: ubuntu-latest
    outputs:
      packages: ${{ steps.filter.outputs.packages }}
      google: ${{ steps.filter.outputs.google }}
      third_party: ${{ steps.filter.outputs.third_party }}
      playbooks: ${{ steps.filter.outputs.playbooks }}
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
    - name: Check for changes
      uses: dorny/paths-filter@v3
      id: filter
      with:
        filters: |
          packages:
            - 'packages/**'
            - '.github/workflows/**'
          google:
            - 'content/response_integrations/google/**'
          third_party:
            - 'content/response_integrations/third_party/**'
          playbooks:
            - 'content/playbooks/**'

  test-mp-windows:
    name: 1. Test mp Package
    needs: changes
    if: ${{ needs.changes.outputs.packages == 'true' }}
    runs-on: windows-latest
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
        cache: 'pip'
    - name: Install dependencies
      run: |
        python -m pip install uv
        uv pip install --system -e ./packages/mp
        uv pip install --system pytest pytest-cov pytest-xdist hypothesis
    - name: Run pytest
      working-directory: packages/mp
      run: python -m pytest tests -n 5 --cov


  validate-integrations-windows:
    name: 2a. Validate Integrations
    needs: changes
    if: ${{ needs.changes.outputs.packages == 'true' || needs.changes.outputs.google == 'true' || needs.changes.outputs.third_party == 'true' }}
    runs-on: windows-latest
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
    - name: Install mp
      run: |
        python -m pip install uv
        uv pip install --system -e ./packages/mp
    - name: Run Validations
      shell: pwsh
      run: |
        wmp config --root-path . --processes 5 --display-config
        wmp validate repository google third_party --only-pre-build

  run-integrations-tests-windows:
    name: 2b. Test Integrations
    needs: validate-integrations-windows
    runs-on: windows-latest
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
    - name: Install mp
      run: |
        python -m pip install uv
        uv pip install --system -e ./packages/mp
    - name: Run integration tests
      shell: pwsh
      run: |
        wmp config --root-path . --processes 5 --display-config
        wmp test --repository third_party --repository google --raise-error-on-violations

  mp-build-integrations-windows:
    name: 2c. Build Integrations
    needs: run-integrations-tests-windows
    runs-on: windows-latest
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
    - name: Install mp
      run: |
        python -m pip install uv
        uv pip install --system -e ./packages/mp
    - name: Build
      shell: pwsh
      run: |
        wmp config --root-path . --processes 5 --display-config
        wmp build repository google third_party 



  validate-playbooks-windows:
    name: 3a. Validate Playbooks
    needs: changes
    if: ${{ needs.changes.outputs.packages == 'true' || needs.changes.outputs.playbooks == 'true' }}
    runs-on: windows-latest
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
    - name: Install mp
      run: |
        python -m pip install uv
        uv pip install --system -e ./packages/mp
    - name: Run Validations
      shell: pwsh
      run: |
        wmp config --root-path . --processes 5 --display-config
        wmp validate repository playbooks --only-pre-build

  mp-build-playbooks-windows:
    name: 3b. Build Playbooks
    needs: validate-playbooks-windows
    runs-on: windows-latest
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
    - name: Install mp
      run: |
        python -m pip install uv
        uv pip install --system -e ./packages/mp
    - name: Build
      shell: pwsh
      run: |
        wmp config --root-path . --processes 5 --display-config
        wmp build repository playbooks