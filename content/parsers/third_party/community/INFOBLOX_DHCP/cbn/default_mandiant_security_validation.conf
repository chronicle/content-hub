# Product: MANDIANT_SECURITY_VALIDATION
# Category: N/A
# Supported Format: JSON
# Reference: N/A
# Last Updated: 2026-06-19
# Copyright 2026 Google SecOps

filter {
  mutate {
    replace => {
      "has_principal" => "false"
      "credential_type" => ""
      "is_authenticated" => ""
      "is_counterparty" => ""
      "endpoint1" => ""
      "endpoint2" => ""
      "authenticate_id" => ""
      "credential_value" => ""
      "adapter_name" => ""
      "principal_id" => ""
      "event_time" => ""
      "protocol" => ""
      "session_id" => ""
      "security_result" => ""
      "principal" => ""
      "organization_uuid" => ""
      "db_uuid" => ""
      "payload" => ""
      "remote_data_org_id" => ""
      "remote_data_org_uuid" => ""
      "remote_data_db_uuid" => ""
      "pipeline_id" => ""
      "pipeline_job_id" => ""
      "pipeline_org_id" => ""
      "pipeline_created_at" => ""
      "pipeline_delivery_status" => ""
      "job_id" => ""
      "job_status" => ""
      "job_progress" => ""
      "job_desc" => ""
      "action_id" => ""
      "action_sim_action_id" => ""
      "action_blocked" => ""
      "action_detected" => ""
      "action_alerted" => ""
      "action_status" => ""
      "action_type" => ""
      "node_id" => ""
      "node_mgmt_ip" => ""
      "node_name" => ""
      "node_security_zone_id" => ""
      "node_sim_ip" => ""
      "node_os_base" => ""
      "node_type" => ""
      "sim_action_id" => ""
      "sim_action_name" => ""
      "sim_action_type" => ""
      "sim_action_vid" => ""
      "zone_id" => ""
      "zone_name" => ""
      "remote_data_published_at" => ""
      "published_at_tmp" => ""
      "ts_prefix" => ""
      "ts_suffix" => ""
      "published_at_clean" => ""
    }
  }
  mutate {
    gsub => ["message", "\\r\\n\\s+", ""]
  }
  json {
    source => "message"
    array_function => "split_columns"
    on_error => "json_parsing_failed"
  }

  if [json_parsing_failed] {
    drop {
      tag => "TAG_MALFORMED_MESSAGE"
    }
  }

  if [organization_uuid] != "" {
    mutate {
      replace => {
        "org_uuid_label.key" => "organization_uuid"
        "org_uuid_label.value.string_value" => "%{organization_uuid}"
      }
      on_error => "org_uuid_label_error"
    }
    if ![org_uuid_label_error] {
      mutate {
        merge => {
          "event.idm.read_only_udm.additional.fields" => "org_uuid_label"
        }
        on_error => "org_uuid_label_merge_error"
      }
    }
  }

  if [db_uuid] != "" {
    mutate {
      replace => {
        "db_uuid_label.key" => "db_uuid"
        "db_uuid_label.value.string_value" => "%{db_uuid}"
      }
      on_error => "db_uuid_label_error"
    }
    if ![db_uuid_label_error] {
      mutate {
        merge => {
          "event.idm.read_only_udm.additional.fields" => "db_uuid_label"
        }
        on_error => "db_uuid_label_merge_error"
      }
    }
  }


  mutate {
    convert => {
      "payload.remote_data.organization_id" => "string"
    }
    on_error => "remote_data_org_id_convert_error"
  }

  mutate {
    replace => {
      "remote_data_org_id" => "%{payload.remote_data.organization_id}"
    }
    on_error => "remote_data_org_id_error"
  }

  if [remote_data_org_id] != "" and ![remote_data_org_id_error] {
    mutate {
      replace => {
        "remote_data_org_id_label.key" => "remote_data_organization_id"
        "remote_data_org_id_label.value.string_value" => "%{remote_data_org_id}"
      }
      on_error => "remote_data_org_id_label_error"
    }
    if ![remote_data_org_id_label_error] {
      mutate {
        merge => {
          "event.idm.read_only_udm.additional.fields" => "remote_data_org_id_label"
        }
        on_error => "remote_data_org_id_label_merge_error"
      }
    }
  }

  mutate {
    replace => {
      "remote_data_org_uuid" => "%{payload.remote_data.organization_uuid}"
    }
    on_error => "remote_data_org_uuid_error"
  }

  if [remote_data_org_uuid] != "" and ![remote_data_org_uuid_error] {
    mutate {
      replace => {
        "remote_data_org_uuid_label.key" => "remote_data_organization_uuid"
        "remote_data_org_uuid_label.value.string_value" => "%{remote_data_org_uuid}"
      }
      on_error => "remote_data_org_uuid_label_error"
    }
    if ![remote_data_org_uuid_label_error] {
      mutate {
        merge => {
          "event.idm.read_only_udm.additional.fields" => "remote_data_org_uuid_label"
        }
        on_error => "remote_data_org_uuid_label_merge_error"
      }
    }
  }

  mutate {
    replace => {
      "remote_data_db_uuid" => "%{payload.remote_data.db_uuid}"
    }
    on_error => "remote_data_db_uuid_error"
  }

  if [remote_data_db_uuid] != "" and ![remote_data_db_uuid_error] {
    mutate {
      replace => {
        "remote_data_db_uuid_label.key" => "remote_data_db_uuid"
        "remote_data_db_uuid_label.value.string_value" => "%{remote_data_db_uuid}"
      }
      on_error => "remote_data_db_uuid_label_error"
    }
    if ![remote_data_db_uuid_label_error] {
      mutate {
        merge => {
          "event.idm.read_only_udm.additional.fields" => "remote_data_db_uuid_label"
        }
        on_error => "remote_data_db_uuid_label_merge_error"
      }
    }
  }

  mutate {
    replace => {
      "published_at_tmp" => "%{payload.remote_data.published_at}"
    }
    on_error => "published_at_tmp_replace_error"
  }
  if ![published_at_tmp_replace_error] {
    date {
      match => ["published_at_tmp", "ISO8601", "yyyy-MM-ddTHH:mm:ss.SSSZZ", "yyyy-MM-ddTHH:mm:ss.SSSZ"]
      target => "event.idm.read_only_udm.metadata.event_timestamp"
      on_error => "published_at_date_error"
    }
  }

  for i, pipeline in payload.published_job_pipelines {
    mutate {
      convert => {
        "i" => "string"
      }
      on_error => "pipeline_i_convert_error"
    }
    mutate {
      convert => {
        "pipeline.id" => "string"
      }
      on_error => "pipeline_id_convert_error"
    }
    mutate {
      convert => {
        "pipeline.job_id" => "string"
      }
      on_error => "pipeline_job_id_convert_error"
    }
    mutate {
      convert => {
        "pipeline.organization_id" => "string"
      }
      on_error => "pipeline_org_id_convert_error"
    }

    mutate {
      replace => {
        "pipeline_id" => "%{pipeline.id}"
      }
      on_error => "pipeline_id_error"
    }
    if [pipeline_id] != "" and ![pipeline_id_error] {
      mutate {
        replace => {
          "pipeline_id_label.key" => "published_job_pipeline_%{i}_id"
          "pipeline_id_label.value.string_value" => "%{pipeline_id}"
        }
        on_error => "pipeline_id_label_error"
      }
      if ![pipeline_id_label_error] {
        mutate {
          merge => {
            "event.idm.read_only_udm.additional.fields" => "pipeline_id_label"
          }
          on_error => "pipeline_id_label_merge_error"
        }
      }
    }

    mutate {
      replace => {
        "pipeline_job_id" => "%{pipeline.job_id}"
      }
      on_error => "pipeline_job_id_error"
    }
    if [pipeline_job_id] != "" and ![pipeline_job_id_error] {
      mutate {
        replace => {
          "pipeline_job_id_label.key" => "published_job_pipeline_%{i}_job_id"
          "pipeline_job_id_label.value.string_value" => "%{pipeline_job_id}"
        }
        on_error => "pipeline_job_id_label_error"
      }
      if ![pipeline_job_id_label_error] {
        mutate {
          merge => {
            "event.idm.read_only_udm.additional.fields" => "pipeline_job_id_label"
          }
          on_error => "pipeline_job_id_label_merge_error"
        }
      }
    }

    mutate {
      replace => {
        "pipeline_org_id" => "%{pipeline.organization_id}"
      }
      on_error => "pipeline_org_id_error"
    }
    if [pipeline_org_id] != "" and ![pipeline_org_id_error] {
      mutate {
        replace => {
          "pipeline_org_id_label.key" => "published_job_pipeline_%{i}_organization_id"
          "pipeline_org_id_label.value.string_value" => "%{pipeline_org_id}"
        }
        on_error => "pipeline_org_id_label_error"
      }
      if ![pipeline_org_id_label_error] {
        mutate {
          merge => {
            "event.idm.read_only_udm.additional.fields" => "pipeline_org_id_label"
          }
          on_error => "pipeline_org_id_label_merge_error"
        }
      }
    }

    mutate {
      replace => {
        "pipeline_created_at" => "%{pipeline.created_at}"
      }
      on_error => "pipeline_created_at_error"
    }
    if [pipeline_created_at] != "" and ![pipeline_created_at_error] {
      mutate {
        replace => {
          "pipeline_created_at_label.key" => "published_job_pipeline_%{i}_created_at"
          "pipeline_created_at_label.value.string_value" => "%{pipeline_created_at}"
        }
        on_error => "pipeline_created_at_label_error"
      }
      if ![pipeline_created_at_label_error] {
        mutate {
          merge => {
            "event.idm.read_only_udm.additional.fields" => "pipeline_created_at_label"
          }
          on_error => "pipeline_created_at_label_merge_error"
        }
      }
    }

    mutate {
      replace => {
        "pipeline_delivery_status" => "%{pipeline.delivery_status}"
      }
      on_error => "pipeline_status_error"
    }
    if [pipeline_delivery_status] != "" and ![pipeline_status_error] {
      mutate {
        replace => {
          "pipeline_status_label.key" => "published_job_pipeline_%{i}_delivery_status"
          "pipeline_status_label.value.string_value" => "%{pipeline_delivery_status}"
        }
        on_error => "pipeline_status_label_error"
      }
      if ![pipeline_status_label_error] {
        mutate {
          merge => {
            "event.idm.read_only_udm.additional.fields" => "pipeline_status_label"
          }
          on_error => "pipeline_status_label_merge_error"
        }
      }
    }
  }

  for i, job in payload.jobs {
    mutate {
      convert => {
        "i" => "string"
      }
      on_error => "job_i_convert_error"
    }
    mutate {
      convert => {
        "job.id" => "string"
      }
      on_error => "job_id_convert_error"
    }

    mutate {
      replace => {
        "job_id" => "%{job.id}"
      }
      on_error => "job_id_error"
    }
    if [job_id] != "" and ![job_id_error] {
      mutate {
        replace => {
          "job_id_label.key" => "job_%{i}_id"
          "job_id_label.value.string_value" => "%{job_id}"
        }
        on_error => "job_id_label_error"
      }
      if ![job_id_label_error] {
        mutate {
          merge => {
            "event.idm.read_only_udm.additional.fields" => "job_id_label"
          }
          on_error => "job_id_label_merge_error"
        }
      }
    }

    mutate {
      replace => {
        "job_status" => "%{job.status}"
      }
      on_error => "job_status_error"
    }
    if [job_status] != "" and ![job_status_error] {
      mutate {
        replace => {
          "job_status_label.key" => "job_%{i}_status"
          "job_status_label.value.string_value" => "%{job_status}"
        }
        on_error => "job_status_label_error"
      }
      if ![job_status_label_error] {
        mutate {
          merge => {
            "event.idm.read_only_udm.additional.fields" => "job_status_label"
          }
          on_error => "job_status_label_merge_error"
        }
      }
    }

    mutate {
      replace => {
        "job_progress" => "%{job.progress}"
      }
      on_error => "job_progress_error"
    }
    if [job_progress] != "" and ![job_progress_error] {
      mutate {
        replace => {
          "job_progress_label.key" => "job_%{i}_progress"
          "job_progress_label.value.string_value" => "%{job_progress}"
        }
        on_error => "job_progress_label_error"
      }
      if ![job_progress_label_error] {
        mutate {
          merge => {
            "event.idm.read_only_udm.additional.fields" => "job_progress_label"
          }
          on_error => "job_progress_label_merge_error"
        }
      }
    }

    mutate {
      replace => {
        "job_desc" => "%{job.desc}"
      }
      on_error => "job_desc_error"
    }
    if [job_desc] != "" and ![job_desc_error] {
      mutate {
        replace => {
          "job_desc_label.key" => "job_%{i}_desc"
          "job_desc_label.value.string_value" => "%{job_desc}"
        }
        on_error => "job_desc_label_error"
      }
      if ![job_desc_label_error] {
        mutate {
          merge => {
            "event.idm.read_only_udm.additional.fields" => "job_desc_label"
          }
          on_error => "job_desc_label_merge_error"
        }
      }
    }
  }

  for i, action in payload.job_actions {
    mutate {
      convert => {
        "i" => "string"
      }
      on_error => "action_i_convert_error"
    }
    mutate {
      convert => {
        "action.id" => "string"
      }
      on_error => "action_id_convert_error"
    }
    mutate {
      convert => {
        "action.sim_action_id" => "string"
      }
      on_error => "action_sim_id_convert_error"
    }
    mutate {
      convert => {
        "action.blocked" => "string"
      }
      on_error => "action_blocked_convert_error"
    }
    mutate {
      convert => {
        "action.detected" => "string"
      }
      on_error => "action_detected_convert_error"
    }
    mutate {
      convert => {
        "action.alerted" => "string"
      }
      on_error => "action_alerted_convert_error"
    }

    mutate {
      replace => {
        "action_id" => "%{action.id}"
      }
      on_error => "action_id_error"
    }
    if [action_id] != "" and ![action_id_error] {
      mutate {
        replace => {
          "action_id_label.key" => "job_action_%{i}_id"
          "action_id_label.value.string_value" => "%{action_id}"
        }
        on_error => "action_id_label_error"
      }
      if ![action_id_label_error] {
        mutate {
          merge => {
            "event.idm.read_only_udm.additional.fields" => "action_id_label"
          }
          on_error => "action_id_label_merge_error"
        }
      }
    }

    mutate {
      replace => {
        "action_sim_action_id" => "%{action.sim_action_id}"
      }
      on_error => "action_sim_action_id_error"
    }
    if [action_sim_action_id] != "" and ![action_sim_action_id_error] {
      mutate {
        replace => {
          "action_sim_action_id_label.key" => "job_action_%{i}_sim_action_id"
          "action_sim_action_id_label.value.string_value" => "%{action_sim_action_id}"
        }
        on_error => "action_sim_action_id_label_error"
      }
      if ![action_sim_action_id_label_error] {
        mutate {
          merge => {
            "event.idm.read_only_udm.additional.fields" => "action_sim_action_id_label"
          }
          on_error => "action_sim_action_id_label_merge_error"
        }
      }
    }

    mutate {
      replace => {
        "action_blocked" => "%{action.blocked}"
      }
      on_error => "action_blocked_error"
    }
    if [action_blocked] != "" and ![action_blocked_error] {
      mutate {
        replace => {
          "action_blocked_label.key" => "job_action_%{i}_blocked"
          "action_blocked_label.value.string_value" => "%{action_blocked}"
        }
        on_error => "action_blocked_label_error"
      }
      if ![action_blocked_label_error] {
        mutate {
          merge => {
            "event.idm.read_only_udm.additional.fields" => "action_blocked_label"
          }
          on_error => "action_blocked_label_merge_error"
        }
      }
    }

    mutate {
      replace => {
        "action_detected" => "%{action.detected}"
      }
      on_error => "action_detected_error"
    }
    if [action_detected] != "" and ![action_detected_error] {
      mutate {
        replace => {
          "action_detected_label.key" => "job_action_%{i}_detected"
          "action_detected_label.value.string_value" => "%{action_detected}"
        }
        on_error => "action_detected_label_error"
      }
      if ![action_detected_label_error] {
        mutate {
          merge => {
            "event.idm.read_only_udm.additional.fields" => "action_detected_label"
          }
          on_error => "action_detected_label_merge_error"
        }
      }
    }

    mutate {
      replace => {
        "action_alerted" => "%{action.alerted}"
      }
      on_error => "action_alerted_error"
    }
    if [action_alerted] != "" and ![action_alerted_error] {
      mutate {
        replace => {
          "action_alerted_label.key" => "job_action_%{i}_alerted"
          "action_alerted_label.value.string_value" => "%{action_alerted}"
        }
        on_error => "action_alerted_label_error"
      }
      if ![action_alerted_label_error] {
        mutate {
          merge => {
            "event.idm.read_only_udm.additional.fields" => "action_alerted_label"
          }
          on_error => "action_alerted_label_merge_error"
        }
      }
    }

    mutate {
      replace => {
        "action_status" => "%{action.status}"
      }
      on_error => "action_status_error"
    }
    if [action_status] != "" and ![action_status_error] {
      mutate {
        replace => {
          "action_status_label.key" => "job_action_%{i}_status"
          "action_status_label.value.string_value" => "%{action_status}"
        }
        on_error => "action_status_label_error"
      }
      if ![action_status_label_error] {
        mutate {
          merge => {
            "event.idm.read_only_udm.additional.fields" => "action_status_label"
          }
          on_error => "action_status_label_merge_error"
        }
      }
    }

    mutate {
      replace => {
        "action_type" => "%{action.action_type}"
      }
      on_error => "action_type_error"
    }
    if [action_type] != "" and ![action_type_error] {
      mutate {
        replace => {
          "action_type_label.key" => "job_action_%{i}_type"
          "action_type_label.value.string_value" => "%{action_type}"
        }
        on_error => "action_type_label_error"
      }
      if ![action_type_label_error] {
        mutate {
          merge => {
            "event.idm.read_only_udm.additional.fields" => "action_type_label"
          }
          on_error => "action_type_label_merge_error"
        }
      }
    }
  }

  for i, node in payload.nodes {
    mutate {
      convert => {
        "i" => "string"
      }
      on_error => "node_i_convert_error"
    }
    mutate {
      convert => {
        "node.id" => "string"
      }
      on_error => "node_id_convert_error"
    }
    mutate {
      convert => {
        "node.security_zone_id" => "string"
      }
      on_error => "node_zone_id_convert_error"
    }

    mutate {
      replace => {
        "node_id" => "%{node.id}"
      }
      on_error => "node_id_error"
    }
    if [node_id] != "" and ![node_id_error] {
      mutate {
        replace => {
          "node_id_label.key" => "node_%{i}_id"
          "node_id_label.value.string_value" => "%{node_id}"
        }
        on_error => "node_id_label_error"
      }
      if ![node_id_label_error] {
        mutate {
          merge => {
            "event.idm.read_only_udm.additional.fields" => "node_id_label"
          }
          on_error => "node_id_label_merge_error"
        }
      }
    }

    mutate {
      replace => {
        "node_mgmt_ip" => "%{node.mgmt_ip}"
      }
      on_error => "node_mgmt_ip_replace_error"
    }
    if [node_mgmt_ip] != "" and ![node_mgmt_ip_replace_error] {
      grok {
        match => {
          "node_mgmt_ip" => "%{IP:node_mgmt_ip}"
        }
          overwrite => ["node_mgmt_ip"]
        on_error => "node_mgmt_ip_grok_error"
      }
      if ![node_mgmt_ip_grok_error] {
        mutate {
          merge => {
            "event.idm.read_only_udm.principal.ip" => "node_mgmt_ip"
            "event.idm.read_only_udm.principal.asset.ip" => "node_mgmt_ip"
          }
          on_error => "node_mgmt_ip_merge_error"
        }
        if ![node_mgmt_ip_merge_error] {
          mutate {
            replace => {
              "has_principal" => "true"
            }
          }
        }
      }
    }

    mutate {
      replace => {
        "node_name" => "%{node.name}"
      }
      on_error => "node_name_error"
    }
    if [node_name] != "" and ![node_name_error] {
      mutate {
        replace => {
          "node_name_label.key" => "node_%{i}_name"
          "node_name_label.value.string_value" => "%{node_name}"
        }
        on_error => "node_name_label_error"
      }
      if ![node_name_label_error] {
        mutate {
          merge => {
            "event.idm.read_only_udm.additional.fields" => "node_name_label"
          }
          on_error => "node_name_label_merge_error"
        }
      }
    }

    mutate {
      replace => {
        "node_security_zone_id" => "%{node.security_zone_id}"
      }
      on_error => "node_security_zone_id_error"
    }
    if [node_security_zone_id] != "" and ![node_security_zone_id_error] {
      mutate {
        replace => {
          "node_security_zone_id_label.key" => "node_%{i}_security_zone_id"
          "node_security_zone_id_label.value.string_value" => "%{node_security_zone_id}"
        }
        on_error => "node_security_zone_id_label_error"
      }
      if ![node_security_zone_id_label_error] {
        mutate {
          merge => {
            "event.idm.read_only_udm.additional.fields" => "node_security_zone_id_label"
          }
          on_error => "node_security_zone_id_label_merge_error"
        }
      }
    }

    mutate {
      replace => {
        "node_sim_ip" => "%{node.sim_ip}"
      }
      on_error => "node_sim_ip_error"
    }
    if [node_sim_ip] != "" and ![node_sim_ip_error] {
      mutate {
        replace => {
          "node_sim_ip_label.key" => "node_%{i}_sim_ip"
          "node_sim_ip_label.value.string_value" => "%{node_sim_ip}"
        }
        on_error => "node_sim_ip_label_error"
      }
      if ![node_sim_ip_label_error] {
        mutate {
          merge => {
            "event.idm.read_only_udm.additional.fields" => "node_sim_ip_label"
          }
          on_error => "node_sim_ip_label_merge_error"
        }
      }
    }

    mutate {
      replace => {
        "node_os_base" => "%{node.os_base}"
      }
      on_error => "node_os_base_error"
    }
    if [node_os_base] != "" and ![node_os_base_error] {
      mutate {
        replace => {
          "node_os_label.key" => "node_%{i}_os_base"
          "node_os_label.value.string_value" => "%{node_os_base}"
        }
        on_error => "node_os_label_error"
      }
      if ![node_os_label_error] {
        mutate {
          merge => {
            "event.idm.read_only_udm.additional.fields" => "node_os_label"
          }
          on_error => "node_os_label_merge_error"
        }
      }
    }

    mutate {
      replace => {
        "node_type" => "%{node.node_type}"
      }
      on_error => "node_type_error"
    }
    if [node_type] != "" and ![node_type_error] {
      mutate {
        replace => {
          "node_type_label.key" => "node_%{i}_type"
          "node_type_label.value.string_value" => "%{node_type}"
        }
        on_error => "node_type_label_error"
      }
      if ![node_type_label_error] {
        mutate {
          merge => {
            "event.idm.read_only_udm.additional.fields" => "node_type_label"
          }
          on_error => "node_type_label_merge_error"
        }
      }
    }
  }

  for i, sim_action in payload.sim_actions {
    mutate {
      convert => {
        "i" => "string"
      }
      on_error => "sim_action_i_convert_error"
    }
    mutate {
      convert => {
        "sim_action.id" => "string"
      }
      on_error => "sim_action_id_convert_error"
    }

    mutate {
      replace => {
        "sim_action_id" => "%{sim_action.id}"
      }
      on_error => "sim_action_id_error"
    }
    if [sim_action_id] != "" and ![sim_action_id_error] {
      mutate {
        replace => {
          "sim_action_id_label.key" => "sim_action_%{i}_id"
          "sim_action_id_label.value.string_value" => "%{sim_action_id}"
        }
        on_error => "sim_action_id_label_error"
      }
      if ![sim_action_id_label_error] {
        mutate {
          merge => {
            "event.idm.read_only_udm.additional.fields" => "sim_action_id_label"
          }
          on_error => "sim_action_id_label_merge_error"
        }
      }
    }

    mutate {
      replace => {
        "sim_action_name" => "%{sim_action.name}"
      }
      on_error => "sim_action_name_error"
    }
    if [sim_action_name] != "" and ![sim_action_name_error] {
      mutate {
        replace => {
          "sim_action_name_label.key" => "sim_action_%{i}_name"
          "sim_action_name_label.value.string_value" => "%{sim_action_name}"
        }
        on_error => "sim_action_name_label_error"
      }
      if ![sim_action_name_label_error] {
        mutate {
          merge => {
            "event.idm.read_only_udm.additional.fields" => "sim_action_name_label"
          }
          on_error => "sim_action_name_label_merge_error"
        }
      }
    }

    mutate {
      replace => {
        "sim_action_type" => "%{sim_action.action_type}"
      }
      on_error => "sim_action_type_error"
    }
    if [sim_action_type] != "" and ![sim_action_type_error] {
      mutate {
        replace => {
          "sim_action_type_label.key" => "sim_action_%{i}_type"
          "sim_action_type_label.value.string_value" => "%{sim_action_type}"
        }
        on_error => "sim_action_type_label_error"
      }
      if ![sim_action_type_label_error] {
        mutate {
          merge => {
            "event.idm.read_only_udm.additional.fields" => "sim_action_type_label"
          }
          on_error => "sim_action_type_label_merge_error"
        }
      }
    }

    mutate {
      replace => {
        "sim_action_vid" => "%{sim_action.vid}"
      }
      on_error => "sim_action_vid_error"
    }
    if [sim_action_vid] != "" and ![sim_action_vid_error] {
      mutate {
        replace => {
          "sim_action_vid_label.key" => "sim_action_%{i}_vid"
          "sim_action_vid_label.value.string_value" => "%{sim_action_vid}"
        }
        on_error => "sim_action_vid_label_error"
      }
      if ![sim_action_vid_label_error] {
        mutate {
          merge => {
            "event.idm.read_only_udm.additional.fields" => "sim_action_vid_label"
          }
          on_error => "sim_action_vid_label_merge_error"
        }
      }
    }
  }

  for i, zone in payload.security_zones {
    mutate {
      convert => {
        "i" => "string"
      }
      on_error => "zone_i_convert_error"
    }
    mutate {
      convert => {
        "zone.id" => "string"
      }
      on_error => "zone_id_convert_error"
    }

    mutate {
      replace => {
        "zone_id" => "%{zone.id}"
      }
      on_error => "zone_id_error"
    }
    if [zone_id] != "" and ![zone_id_error] {
      mutate {
        replace => {
          "zone_id_label.key" => "security_zone_%{i}_id"
          "zone_id_label.value.string_value" => "%{zone_id}"
        }
        on_error => "zone_id_label_error"
      }
      if ![zone_id_label_error] {
        mutate {
          merge => {
            "event.idm.read_only_udm.additional.fields" => "zone_id_label"
          }
          on_error => "zone_id_label_merge_error"
        }
      }
    }

    mutate {
      replace => {
        "zone_name" => "%{zone.name}"
      }
      on_error => "zone_name_error"
    }
    if [zone_name] != "" and ![zone_name_error] {
      mutate {
        replace => {
          "zone_name_label.key" => "security_zone_%{i}_name"
          "zone_name_label.value.string_value" => "%{zone_name}"
        }
        on_error => "zone_name_label_error"
      }
      if ![zone_name_label_error] {
        mutate {
          merge => {
            "event.idm.read_only_udm.additional.fields" => "zone_name_label"
          }
          on_error => "zone_name_label_merge_error"
        }
      }
    }
  }
  mutate {
    replace => {
      "event.idm.read_only_udm.metadata.vendor_name" => "MANDIANT"
      "event.idm.read_only_udm.metadata.product_name" => "MANDIANT SECURITY VALIDATION"
    }
  }


  if [security_result] != "" {
    mutate {
      merge => {
        "event.idm.read_only_udm.security_result" => "security_result"
      }
      on_error => "security_result_error"
    }
  }
  if [principal] != "" {
    mutate {
      rename => {
        "principal" => "event.idm.read_only_udm.principal"
      }
    }
  }

  if [has_principal] == "true" {
    mutate {
      replace => {
        "event.idm.read_only_udm.metadata.event_type" => "STATUS_UPDATE"
      }
    }
  }
  else {
    mutate {
      replace => {
        "event.idm.read_only_udm.metadata.event_type" => "GENERIC_EVENT"
      }
    }
  }

  mutate {
    merge => { "@output" => "event" }
  }
}
