-   description: "DTM-specific alert scoring. Reads the raw alert's native `severity` field (from the Main Alert event) and writes both a case-context tag and a weighted scoring entry reflecting it, keyed consistently by the constant context-variable name `CONST_CONTEXT_ALERT_SEVERITY` set to `CTX_ALERT_DTM_SEVERITY`."
    version: 1.0
    item_name: DTM Alert Score by Severity
    item_type: Block
    publish_time: '2026-08-22'
    new: true
    regressive: false
    deprecated: false
    removed: false
