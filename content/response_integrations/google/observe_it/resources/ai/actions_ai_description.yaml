Ping:
  action_product_categories:
    add_alert_comment: false
    add_ioc_to_allowlist: false
    add_ioc_to_blocklist: false
    contain_host: false
    create_ticket: false
    delete_email: false
    disable_identity: false
    download_file: false
    enable_identity: false
    enrich_asset: false
    enrich_ioc: false
    execute_command_on_the_host: false
    get_alert_information: false
    reasoning: >-
      The action is a diagnostic connectivity test ('Ping'). It does not perform any
      of the defined operational tasks such as enrichment, containment, ticketing,
      or alert management.
    remove_ioc_from_allowlist: false
    remove_ioc_from_blocklist: false
    reset_identity_password: false
    search_asset: false
    search_email: false
    search_events: false
    send_email: false
    send_message: false
    submit_file: false
    uncontain_host: false
    update_alert: false
    update_email: false
    update_identity: false
    update_ticket: false
  ai_description: >-
    ### General Description

    The **Ping** action is a diagnostic tool designed to verify the connectivity between
    Google SecOps and the ObserveIT server. It validates the provided configuration
    parameters, including the API Root and OAuth2 credentials, by attempting to authenticate
    and reach a health check endpoint.


    ### Parameters Description

    | Parameter | Type | Mandatory | Description |

    | :--- | :--- | :--- | :--- |

    | API Root | String | Yes | The base URL of the ObserveIT instance (e.g., `https://observeit.example.com`).
    |

    | Client ID | String | Yes | The Client ID required for OAuth2 authentication.
    |

    | Client Secret | String | Yes | The Client Secret required for OAuth2 authentication.
    |

    | Verify SSL | Boolean | Yes | Determines whether to verify the SSL certificate
    of the ObserveIT server. |


    ### Additional Notes

    This action does not process entities or modify any data. It is typically used
    during the initial setup of the ObserveIT integration or for troubleshooting communication
    issues.


    ### Flow Description

    1. **Configuration Retrieval**: The action extracts the API Root, Client ID, Client
    Secret, and SSL verification settings from the integration's configuration.

    2. **Authentication**: It initializes the `ObserveITManager`, which triggers an
    OAuth2 token request to the `/v2/apis/auth/oauth/token` endpoint using the provided
    credentials.

    3. **Health Check**: Upon successful authentication, the action performs a GET
    request to the ObserveIT health check endpoint (`/v2/apis/report;realm=observeit/_health`).

    4. **Status Validation**: The action checks if the health check response returns
    a status code of 200. If successful, it reports a positive connection; otherwise,
    it catches exceptions and reports a failure.
  capabilities:
    can_create_case_comments: false
    can_create_insight: false
    can_modify_alert_data: false
    can_mutate_external_data: false
    can_mutate_internal_data: false
    can_update_entities: false
    external_data_mutation_explanation: null
    fetches_data: false
    internal_data_mutation_explanation: null
    reasoning: >-
      The action performs a connectivity test (GET request) to verify the integration
      setup. It does not fetch contextual data about entities or alerts, nor does
      it modify any external or internal systems.
  categories:
    enrichment: false
    reasoning: >-
      The action is named 'Ping'. Per the instructions, 'Actions named Ping must not
      be categorized as enrichment actions.' Furthermore, it does not fetch contextual
      data about entities, failing the primary purpose criteria for enrichment.
  entity_usage:
    entity_types: []
    filters_by_additional_properties: false
    filters_by_alert_identifier: false
    filters_by_case_identifier: false
    filters_by_creation_time: false
    filters_by_entity_type: false
    filters_by_identifier: false
    filters_by_is_artifact: false
    filters_by_is_enriched: false
    filters_by_is_internal: false
    filters_by_is_pivot: false
    filters_by_is_suspicious: false
    filters_by_is_vulnerable: false
    filters_by_modification_time: false
    reasoning: >-
      The action does not interact with the `target_entities` list or any entity-specific
      identifiers.
