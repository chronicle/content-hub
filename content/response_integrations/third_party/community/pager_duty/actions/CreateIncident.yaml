creator: tcondello
default_result_value: ''
description: 'Create a new incident with the given parameters.

    '
dynamic_results_metadata:
-   result_example_path: resources/CreateIncident_JsonResult_example.json
    result_name: JsonResult
    show_result: true
integration_identifier: PagerDuty
name: Create Incident
parameters:
-   default_value: A disk is getting full on this machine. You should investigate
        what is causing the disk to fill, and ensure that there is an automated process
        in place for ensuring data is rotated (eg. logs should have logrotate around
        them). If data is expected to stay on this disk forever, you should start
        planning to scale up to a larger disk.
    description: Details and body text describing the incident.
    is_mandatory: true
    name: Details
    type: string
-   default_value: The server is on fire.
    description: Title or summary of the incident.
    is_mandatory: true
    name: Title
    type: string
-   default_value: tcondello@siemplify.co
    description: Email address of the user creating the incident.
    is_mandatory: true
    name: Email
    type: string
-   default_value: high
    description: 'Urgency level of the incident. Possible values: low, high.'
    is_mandatory: true
    name: Urgency
    type: string
script_result_name: ScriptResult
