name: List Similar Devices
description: 'List similar devices to the endpoint in Darktrace. Supported entities:
    IP, Hostname, Mac Address.'
integration_identifier: Darktrace
parameters:
-   name: Max Devices To Return
    default_value: 50
    type: string
    description: 'Specify how many devices to return per entity. Default: 50.'
    is_mandatory: false
dynamic_results_metadata:
-   result_example_path: resources/list_similar_devices_JsonResult_example.json
    result_name: JsonResult
    show_result: true
creator: admin
simulation_data_json: '{"Entities": ["ADDRESS", "HOSTNAME", "MacAddress"]}'
