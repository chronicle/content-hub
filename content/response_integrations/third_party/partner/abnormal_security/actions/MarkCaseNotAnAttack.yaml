name: Mark Case Not An Attack
description: Acknowledge an Abnormal Security case as benign / not an attack. Convenience action for `Post Case Action` with action=acknowledge_not_an_attack hard-coded.
integration_identifier: AbnormalSecurity
entity_types:
- THREATCAMPAIGN
parameters:
-   name: Case ID
    default_value: '[Event.event_extracted_event.abx_body.case_id]'
    type: string
    description: The unique identifier of the case to act on. When the action is run with scope on a ThreatCampaign entity, this resolves to the entity's identifier (the Abnormal case_id).
    is_mandatory: true
dynamic_results_metadata:
-   result_example_path: resources/MarkCaseNotAnAttack_JsonResult_example.json
    result_name: JsonResult
    show_result: true
creator: AbnormalSecurity
