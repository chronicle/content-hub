creator: Rob
default_result_value: ''
description: Analyze Headers accepts a JSON object containing a list of email headers.  The
    action verifies the email's DKIM and ARC signatures and reports the SPF, DKIM, and
    DMARC authentication results recorded for the message in its Authentication-Results
    headers.  It will also check to see if any of the relay servers are denylisted by
    querying multiple RBLs and enrich them with geo-location and IP whois information.
dynamic_results_metadata:
-   result_example_path: resources/AnalyzeHeaders_JsonResult_example.json
    result_name: JsonResult
    show_result: true
integration_identifier: EmailUtilities
name: Analyze Headers
parameters:
-   default_value: '{}'
    description: The JSON object that contains the email headers.
    is_mandatory: true
    name: Headers JSON
    type: string
-   default_value: 'false'
    description: Base the reported authentication results on the Authentication-Results-Original
        header instead of Authentication-Results when the message carries one (falling
        back to Authentication-Results when it does not). Enable this if and only
        if the receiving organization routes mail through a Secure Email Gateway
        that preserves the internet-boundary results in that header (e.g. Proofpoint,
        Cisco Secure Email). Do not enable it for API-based email security products
        (e.g. Abnormal Security) or when no such gateway is in use. The header is
        trivial to forge, so it should only be trusted when your own gateway is known
        to write it.
    is_mandatory: false
    name: Use Authentication-Results-Original Header
    type: boolean
-   default_value: 'false'
    description: Some mail services (e.g. Proton Mail, and other Postfix or Sendmail
        systems running separate OpenDKIM, OpenDMARC, and SPF filters) record each
        authentication method in its own Authentication-Results header rather than
        one combined header (RFC 8601 section 4). Enable this if and only if the
        receiving mail service splits results across separate headers and always
        includes a DMARC result. When disabled (default), AuthenticationSummary is
        based only on the topmost Authentication-Results header (the one stamped
        by the receiving server), so extra headers, including any forged by a sender,
        are ignored; AuthenticationResults and AuthenticationByServer still list
        every header for reference. If summaries look unexpectedly sparse for a provider
        that splits headers, this option is why.
    is_mandatory: false
    name: Allow Multiple Authentication-Results Headers
    type: boolean
script_result_name: ScriptResult
